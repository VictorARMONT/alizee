import { QuizFlow } from "@/funnel/QuizFlow";

export const metadata = {
  title: "alizee — Personaliza el regalo",
  robots: { index: false, follow: false },
};

export default function QuizPage() {
  return <QuizFlow />;
}
